FROM node:24-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 3000

# Use direct node command with experimental TypeScript support
# Environment variables are passed via Docker Compose
CMD ["node", "--experimental-transform-types", "--watch", "src/index.ts"]
